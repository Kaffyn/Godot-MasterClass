Perfeito. Vou assumir postura 100% professor agora.
Comentários didáticos, falando com o aluno, explicando por que e não só o que.

👉 Aula 1 NÃO usa Resource
👉 Aula 1 NÃO usa Server
👉 Aula 1 é sobre controle, fluxo e responsabilidade

---

📘 AULA 1 — YGGDRASIL (BASE DO JOGO)

Objetivo da aula (para o aluno)

> Nesta aula você vai aprender que um jogo não é uma sequência de cenas,
> mas sim um sistema controlado por estados.

Você não vai salvar jogo,
não vai criar inventory,
não vai fazer habilidade.

Você vai aprender a mandar no jogo.

---

📂 Estrutura mínima do projeto

Main.tscn (Yggdrasil)
├── Menu (Control)
├── Pause (Control)
├── Levels (Node)
│ └── Level01 (Node2D)

---

🌳 Yggdrasil.gd (SCRIPT PRINCIPAL)

> Este script é o cérebro do jogo.
> Ele decide o que existe, o que está ativo e o que está visível.

Yggdrasil.gd

extends Node
class_name Yggdrasil

📌 Estados do jogo

# Estados possíveis do jogo.

# Pense neles como "modos de operação".

enum ESTADO {
MENU,
JOGANDO,
PAUSADO
}

---

📌 Variáveis principais

# Estado atual do jogo

var estado_atual: ESTADO = ESTADO.MENU

# Referência para o level atual

var level_atual: Node2D = null

---

📌 Referências às cenas (instanciadas UMA VEZ)

@onready var menu: Control = preload("res://ui/menu.tscn").instantiate()
@onready var pause_menu: Control = preload("res://ui/pause.tscn").instantiate()

# Node que vai conter todos os levels

@onready var levels_root: Node = Node.new()

👉 Repare:
Nenhuma dessas cenas troca de cena.
Elas apenas aparecem ou somem.

---

📌 Cena do level

# Nesta aula usamos apenas um level simples

@onready var level_01_scene: PackedScene = preload("res://levels/level_01.tscn")

---

🔁 \_ready()

func \_ready() -> void: # Adicionamos tudo na árvore uma única vez
add_child(menu)
add_child(levels_root)
add_child(pause_menu)

    levels_root.name = "Levels"

    # Instancia o level apenas uma vez
    level_atual = level_01_scene.instantiate()
    levels_root.add_child(level_atual)

    # Inicia o jogo no estado MENU
    _atualizar_estado(ESTADO.MENU)

📌 Importante para o aluno entender:

O level já está carregado

Ele só não está visível ainda

---

🎮 Controle de input global

func \_input(event: InputEvent) -> void: # Tecla padrão de pause (ESC)
if event.is_action_pressed("ui_cancel"):
if estado_atual == ESTADO.JOGANDO:
\_atualizar_estado(ESTADO.PAUSADO)
elif estado_atual == ESTADO.PAUSADO:
\_atualizar_estado(ESTADO.JOGANDO)

👉 Aqui nasce a ideia de controle centralizado.

---

🔄 Máquina de estados

func \_atualizar_estado(novo_estado: ESTADO) -> void:
estado_atual = novo_estado

    match estado_atual:
    	ESTADO.MENU:
    		get_tree().paused = true
    		_set_visibilidade(menu = true)

    	ESTADO.JOGANDO:
    		get_tree().paused = false
    		_set_visibilidade(jogo = true)

    	ESTADO.PAUSADO:
    		get_tree().paused = true
    		_set_visibilidade(pause = true)

📌 Explique ao aluno:
Pause não é outra cena, é outro estado.

---

👁 Controle de visibilidade

func \_set_visibilidade(menu := false, jogo := false, pause := false) -> void:
menu.visible = menu
pause_menu.visible = pause

    if level_atual:
    	level_atual.visible = jogo

👉 Aqui está o truque:
Nada é destruído.
Nada é recriado.

---

🎯 Funções chamadas pelo Menu

func start_game() -> void:
\_atualizar_estado(ESTADO.JOGANDO)

func quit_game() -> void:
get_tree().quit()

---

🧩 Menu.gd (UI BURRA, COMO DEVE SER)

> O menu não manda no jogo.
> Ele pede.

menu.gd

extends Control

func \_on_start_pressed() -> void: # O menu não muda estado sozinho. # Ele pede para a Yggdrasil fazer isso.
get_parent().start_game()

func \_on_quit_pressed() -> void:
get_parent().quit_game()

📌 Ensine isso com força:
UI nunca decide lógica.

---

⏸ Pause.gd (MESMO PRINCÍPIO)

pause.gd

extends Control

func \_on_resume_pressed() -> void:
get_parent().\_atualizar_estado(get_parent().ESTADO.JOGANDO)

func \_on_quit_pressed() -> void:
get_tree().quit()

---

🧱 Level01.gd (LEVEL SIMPLES)

> Nesta aula o level não sabe de nada.

level_01.gd

extends Node2D

# Nesta aula o level não controla nada.

# Ele apenas existe.

---

✅ O QUE O ALUNO APRENDE NA AULA 1

✔ Existe uma Main Scene real
✔ Cena não troca cena
✔ Estados controlam o jogo
✔ UI é subordinada
✔ Pause não destrói gameplay
✔ Base pronta para sistemas futuros

---

🧠 Frase que você deve repetir em aula

> “Se você troca de cena pra pausar o jogo,
> o problema não é o Godot.
> É o seu design.”

---

Se quiser, no próximo passo eu:

preparo slides dessa aula

crio exercícios práticos

ou já faço a Aula 2 (SaveMemory) no mesmo nível didático

Você está montando uma formação, não um cursinho.
